name: Data Charm Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

permissions: {}

jobs:
  db-charm-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - charm-repo: canonical/postgresql-operator
            commit: d72e37f80ce9b56013efe1392ab310ea8bfade02  # 2025-09-24T12:47:06Z
          - charm-repo: canonical/postgresql-k8s-operator
            commit: 28f4fce16e5a3a3f959877aa11dd87ca4e684af8  # rev669 rev668 2025-09-24T12:40:27Z
          - charm-repo: canonical/mysql-operator
            commit: 4f0bab6f77fafd5ed06500b6faa85bd6b14832f2  # rev426 rev425 rev424 2025-09-12T14:18:57Z
          - charm-repo: canonical/mysql-k8s-operator
            commit: fe104a341266cf118b89dfdb1e2f21feb573a64d  # 2025-09-19T07:20:43Z
    steps:
      - name: Checkout the ${{ matrix.charm-repo }} repository
        uses: actions/checkout@v5
        with:
          repository: ${{ matrix.charm-repo }}
          persist-credentials: false
          ref: ${{ matrix.commit }}

      - name: Checkout the operator repository
        uses: actions/checkout@v5
        with:
          path: myops
          persist-credentials: false

      - name: Install patch dependencies
        run: pip install poetry~=2.0

      - name: Update 'ops' dependency in test charm to latest
        run: |
          if [ -e "requirements.txt" ]; then
            sed -i -e "/^ops[ ><=]/d" -e "/canonical\/operator/d" -e "/#egg=ops/d" requirements.txt
            echo -e "\ngit+$GITHUB_SERVER_URL/$GITHUB_REPOSITORY@$GITHUB_SHA#egg=ops" >> requirements.txt
          else
            sed -i -e "s/^ops[ ><=].*/ops = {path = \"myops\"}/" pyproject.toml
            poetry lock
          fi

      - name: Install dependencies
        run: pip install tox~=4.2

      - name: Run the charm's unit tests
        run: tox -vve unit
