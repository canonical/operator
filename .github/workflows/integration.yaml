name: ops Integration Tests

on:
  workflow_dispatch:
  schedule:
    - cron:  '42 7 25 * *'

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        preset: ['microk8s']  # ['k8s'] https://github.com/jnsgruk/concierge/issues/51
        test-group: [1, 2, 3, 4]

    steps:
      - run: sudo snap install --classic concierge
      - run: >
          sudo concierge prepare
          --juju-channel=3/stable
          --charmcraft-channel=3.x/stable
          -p "${{ matrix.preset }}"
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      # Note that the test group count must match the groups, and there must be enough tests.
      - run: >
          uvx tox -e integration
          --
          --log-cli-level=INFO -s
          --test-group-random-seed=0
          --test-group-count=4
          --test-group=${{ matrix.test-group }}
